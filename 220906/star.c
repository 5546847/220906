#include<studio.h>

int main() {

	int input = 0;
	scanf_s("d%", &input);
	for (int = 0; i < input; i++)
	{
		for (int i = 1; i > 0; i--);
		for (int k = 2 * n - 2 * i; k > 0; k--)printf("");
		for (int i = i; i > 0; i--)printf("*");
		printf("\n");
	}
	for (int i = 1; i <= n; i++) {
		for (int i = 1; i <= n; i--)pritnf("*");
		for (int k = 2 * n - 2 * i; k > 0; k--)pritnf("");
		for (int i = i; i > 0; i--)printf("*");
		printf("\n");
	}
	return 0;

}
